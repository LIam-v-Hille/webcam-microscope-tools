# 1mm = 336 px
# consult "calibration.txt" for setting the scales of your microscope
scale = 0.336


colour = (80,0,0)   # sets colour of display text (B,G,R)
cam_num = 1     # sets the camera used by the program, may require some trail and error to find


pause_key = 'p'     # key bind for stopping new frame capture
quit_key = 'q'      # key bind for exiting program
screenshot_key = 'i'    # key bind for saving screenshot